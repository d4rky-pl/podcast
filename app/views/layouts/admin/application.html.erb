<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Podcastapp" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Podcast" %>">
  <%= stylesheet_link_tag 'admin', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'admin', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body class="controller-<%= params[:controller].parameterize %> action-<%= params[:action].parameterize %>">
<header>
  <%= render partial: 'layouts/admin/navigation' %>
</header>
<main role="main">
  <div class="container">
    <%= render partial: 'layouts/admin/messages' %>
    <%= yield %>
  </div>
</main>
</body>
</html>
